# DCS Statistics Docker Configuration
# Copy this file to .env and adjust as needed

# Proxy configuration (nginx-proxy-manager, simple, or none)
# - nginx-proxy-manager: Full reverse proxy with web UI (port 81 for admin)
# - simple: Basic nginx server (no proxy features)
# - none: No proxy installed (for users with existing proxy)
PROXY_TYPE=nginx-proxy-manager

# Nginx Proxy Manager Database Configuration
# Leave empty to use built-in SQLite database (recommended for most users)
# Fill these out only if you want to use external MySQL/MariaDB
NPM_DB_MYSQL_HOST=
NPM_DB_MYSQL_PORT=3306
NPM_DB_MYSQL_USER=
NPM_DB_MYSQL_PASSWORD=
NPM_DB_MYSQL_NAME=

# IPv6 Support (true to disable IPv6, leave empty to enable)
NPM_DISABLE_IPV6=

# Web server port (default: 9080)
# Note: If using nginx-proxy-manager, this is ignored (uses ports 80/443/81)
WEB_PORT=9080

# Site URL (change this to your actual domain/IP for production)
SITE_URL=http://localhost:9080

# PHP Memory Limit
PHP_MEMORY_LIMIT=256M

# Redis Memory Limit
REDIS_MEMORY_LIMIT=256mb

# Environment (development/production)
APP_ENV=production

# Debug mode (true/false)
APP_DEBUG=false

# Timezone
TZ=UTC